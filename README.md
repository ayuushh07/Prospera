# Prospera — Paper Trading Simulator

A Netlify-ready portfolio demo for a paper trading app. This version runs fully in the browser using JavaScript and localStorage so it can be deployed instantly.

## What it demonstrates
- Stock search UI
- Watchlist management
- Buy/sell form validation
- Portfolio holdings
- Transaction history
- SVG chart rendering
- Local persistence through localStorage

## Run locally
Open `index.html` in your browser.

## Deploy on Netlify
Drag this `prospera` folder to Netlify Drop, or zip the folder and upload it.

## Future full-stack upgrade
Use `schema.sql` and `api-contract.md` to convert this into a real React + Node/Express + PostgreSQL/Supabase project.
